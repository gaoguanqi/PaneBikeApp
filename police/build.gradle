apply from:"../multiple-build.gradle"

rootProject.ext.appBuild = 2

android {
    resourcePrefix "police_" //给 Module 内的资源名增加前缀, 避免资源名冲突, 建议使用 Module 名作为前缀
}


dependencies {

    implementation project(path: ':common')
}
